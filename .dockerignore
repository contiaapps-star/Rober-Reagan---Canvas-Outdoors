.git
.gitignore
.env
.env.*
node_modules
dist
*.db
*.db-journal
*.sqlite
*.sqlite3
db-data/
coverage/
.nyc_output/
.vscode/
.idea/
.DS_Store
Thumbs.db
docs/
resourses/
*.log
README.md
# Playwright artefacts and the test compose file. Tests/ is NOT excluded —
# the dev stage of the Dockerfile copies it for `npm test`. The runner
# stage doesn't copy tests at all, so they're already kept out of prod.
playwright-results/
test-results/
playwright-report/
docker-compose.test.yml

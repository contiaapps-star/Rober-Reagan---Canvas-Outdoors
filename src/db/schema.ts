// Phase 0: schema is intentionally empty.
// Real tables (competitors, competitor_handles, target_keywords, inspiration_sources,
// activities, poll_runs, api_spend_log, users, session_state) land in Phase 1.
// See CLAUDE.md "Modelo de Datos" for the authoritative schema.

export {};

/*!
 * Phase 0 placeholder for htmx 1.9.x.
 *
 * Replace this file with the real minified htmx bundle before Phase 3
 * (when the dashboard starts swapping fragments via hx-get / hx-post).
 *
 * Source:  https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js
 * License: BSD 2-Clause (https://github.com/bigskysoftware/htmx/blob/master/LICENSE)
 *
 * No runtime behavior is wired up in Phase 0; this stub keeps the static
 * asset pipeline honest and avoids a 404 on the dashboard skeleton.
 */
(function () {
  if (typeof window !== 'undefined' && !window.htmx) {
    window.htmx = { __placeholder: true, version: '0.0.0-phase0-stub' };
  }
})();
